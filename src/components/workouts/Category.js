import React from 'react';

const Category = () => {
  return (
    <div>
      category name + edit category name + delete category + whether deleting the category should also delete the workouts
    </div>
  );
};

export default Category;